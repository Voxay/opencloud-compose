---
# only expose the ports when you know what you re doing!
services:
  keycloak:
    ports:
      # expose the keycloak server on all interfaces
      - "0.0.0.0:${KEYCLOAK_FIRST_HOST_PORT:-9000}:9000"
      - "0.0.0.0:${KEYCLOAK_HTTP_HOST_PORT:-8080}:8080"


